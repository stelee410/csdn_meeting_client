<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.csdn.meeting.infrastructure.repository.mapper.MeetingFavoritePOMapper">

    <select id="selectPageByUserIdOrderByCreatedAtDesc" resultType="com.csdn.meeting.infrastructure.po.MeetingFavoritePO">
        SELECT * FROM t_meeting_favorite WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <delete id="deleteByUserIdAndMeetingId">
        DELETE FROM t_meeting_favorite WHERE user_id = #{userId} AND meeting_id = #{meetingId}
    </delete>
</mapper>
